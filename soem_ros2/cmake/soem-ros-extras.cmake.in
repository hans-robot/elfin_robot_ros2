# Intentionally empty.
# The include directory (containing the soem_ros2/ headers) is exported via
# ament_export_include_directories(include) in CMakeLists.txt. The previous
# catkin-era logic here built include paths from @CATKIN_GLOBAL_INCLUDE_DESTINATION@,
# which is undefined under ament and produced an invalid "/include/soem" path.
